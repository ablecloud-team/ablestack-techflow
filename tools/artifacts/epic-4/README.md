# Epic #4 결과 자산 생성

`build_report.py`, `build_presentation.mjs`, `build_presentation_pdf.py`, `build_manifest.py` 순서로 실행하고 `validate_artifacts.py`로 검증한다. 권위 증적은 `docs/evidence/epic-4/production-e2e.json`이다.

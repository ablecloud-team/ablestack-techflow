# 키가 비어 있는 요청은 집계하지 않는다. 로그인·가입·토큰 발급만 제한한다.
map $request_uri $techflow_community_auth_key {
    default "";
    ~^/(login|register|api/token)(/|$) $binary_remote_addr;
}
limit_req_zone $techflow_community_auth_key zone=techflow_community_auth:10m rate=12r/m;
server_tokens off;
